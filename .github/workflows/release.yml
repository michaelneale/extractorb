name: Build and Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-release:
    name: Build and Release
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Build release binary
        run: cargo build --release

      - name: Package binary
        run: |
          mkdir -p dist
          cp target/release/extractorb dist/
          cd dist
          # Create a zip file instead of tar.gz for better compatibility
          zip -r extractorb-macos.zip extractorb
          shasum -a 256 extractorb-macos.zip > extractorb-macos.zip.sha256

      # Delete existing tag and release
      - name: Delete existing tag
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              await github.rest.git.deleteRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: 'tags/latest'
              });
              console.log('Deleted existing tag');
            } catch (error) {
              console.log('Tag does not exist or could not be deleted');
            }

      # Create a new release
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: latest
          name: Latest Build
          body: |
            This is the latest build of extractorb from the main branch.
            
            ## Installation
            
            ```bash
            # Download
            curl -LO https://github.com/michaelneale/extractorb/releases/download/latest/extractorb-macos.zip
            
            # Extract
            unzip extractorb-macos.zip
            
            # Make executable and move to PATH (optional)
            chmod +x extractorb
            sudo mv extractorb /usr/local/bin/
            ```
          files: |
            dist/extractorb-macos.zip
            dist/extractorb-macos.zip.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}